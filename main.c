#include <stdio.h>
#include <stdlib.h>

int main()
{
 int jo;
printf ("enter a number:")
;scanf("%d",&jo);

if(jo % 2 == 0)
    printf("%d is even number.", jo);
else
    printf("%d is an odd number.", jo);
return 0;

}
